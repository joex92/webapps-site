let Complex = require('./src/complex.js');

// This is a relatively small module, so the only necesary
// export is the class for instantiating complex numbers
module.exports = {
  Complex: Complex.Complex
}
